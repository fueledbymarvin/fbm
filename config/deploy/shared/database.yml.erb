<%= fetch(:rails_env) %>:
  adapter: postgresql
  timeout: 5000
  encoding: utf8
  reconnect: false
  database: <%= "#{fetch(:application)}_#{fetch(:rails_env)}" %>
  pool: 5
  username: <%= "#{fetch(:user)}" %>
  password: <%%= ENV['PG_PASSWORD'] %>
  host:
  port: 5432
